name: Retired Production D1 Canary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: retired one-shot production D1 canary
    if: ${{ false }}
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Retired
        run: echo 'The #74 first-production-D1 canary is completed and cannot be replayed. Use the reviewed Control authorization registry for future operations.'

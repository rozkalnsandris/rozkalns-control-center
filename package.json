{
  "name": "rozkalns-control-center",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "cf-typegen": "wrangler types",
    "typecheck": "wrangler types && tsc -b --pretty false",
    "lint": "eslint . --max-warnings=0",
    "test": "rm -rf .tmp-tests && tsc -p tsconfig.test.json && node --test .tmp-tests/tests/*.test.js",
    "build": "wrangler types && tsc -b && vite build",
    "cf:dry-run": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "audit:runtime": "npm audit --omit=dev --audit-level=high",
    "verify:actions": "node scripts/verify-actions-pinning.mjs",
    "verify:public": "node scripts/verify-public-repo-safety.mjs",
    "verify:policy": "npm run verify:actions && npm run verify:public",
    "check": "npm run verify:policy && npm run audit:runtime && npm run typecheck && npm run lint && npm test && npm run build && npm run cf:dry-run"
  },
  "dependencies": {
    "react": "19.2.1",
    "react-dom": "19.2.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.51.1",
    "@eslint/js": "9.39.1",
    "@types/node": "24.10.1",
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.5",
    "eslint": "9.39.2",
    "eslint-plugin-react-hooks": "7.0.1",
    "eslint-plugin-react-refresh": "0.4.24",
    "globals": "16.5.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.48.0",
    "vite": "8.2.1",
    "wrangler": "4.120.0"
  }
}
